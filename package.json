{
  "name": "corelife",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm ci --prefix client --include=dev && npm ci --prefix server --include=dev && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "typecheck": "npm run build --prefix client -- --mode development"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0"
  }
}
